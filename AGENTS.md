# AGENTS.md

Guidance for future agents working in this repository.

## Project Snapshot

- This is a Flutter app named `shadchan`, a Hebrew RTL shidduchim management app.
- The current Dart source layout is intentionally simple and flat:
  - `lib/models/` - Hive data models and generated adapters.
  - `lib/providers/` - `ChangeNotifier` repositories for people and matches.
  - `lib/screens/` - top-level app screens and flows.
  - `lib/services/` - backup/import, contacts import, incoming files, notifications.
  - `lib/utils/` - routing, theme, enums, formatting, phone/share/name helpers.
  - `lib/widgets/` - reusable UI widgets.
  - `lib/dialogs/` - reusable dialogs and sheets.
- App entry points are `lib/main.dart` and `lib/app.dart`.
- Navigation is centralized in `lib/utils/app_router.dart` using `go_router`.
- Tests live under `test/`, with service tests in `test/core/services/` and the app boot test in `test/widget_test.dart`.

## Product Rules

- All user-facing UI text must be Hebrew.
- Keep the app fully RTL. `lib/app.dart` wraps the app in `Directionality.rtl`; new screens and widgets should fit that assumption.
- Preserve local-first behavior. User data is stored locally with Hive, local files, and explicit import/export flows.
- Be careful with privacy-sensitive features: contacts, photos, sharing, backup JSON files, and notifications.
- If a task is unclear or has a meaningful product/technical choice, ask before proceeding.

## Platform Rules

- Keep the minimum platform intent aligned to Android 21 and iOS 15.5 unless the user explicitly approves a change.
- iOS deployment target is set to `15.5` in `ios/Runner.xcodeproj/project.pbxproj` and in `ios/Podfile`. The floor is forced by the dependencies, not by choice: the Firebase pods require 15.0 and `google_mlkit_face_detection` requires 15.5. Both files must be changed together.
- iOS bundle identifier is `com.gilad.shadchan`, matching the Android `applicationId`. It is also the identifier the Firebase iOS app is registered under, so changing it means re-running `flutterfire configure`.
- `ios/Podfile` is hand-written (Windows `flutter create` never generates one). Its `post_install` block sets the permission_handler compile flags: only `PERMISSION_PHOTOS=1`, everything else `0`, so the binary carries no API references without a matching usage description in `Info.plist`. Adding a new `permission_handler` permission means adding both the flag and the plist string.
- Android uses the Flutter Gradle values in `android/app/build.gradle.kts`; do not raise the effective min SDK casually.
- Android release builds use `android/app/proguard-rules.pro` and `android/app/src/main/res/raw/keep.xml` for notification/Gson/R8 safety.
- Native file-open backup import is wired through:
  - Android: `android/app/src/main/kotlin/com/gilad/shadchan/MainActivity.kt` and `AndroidManifest.xml`.
  - iOS: `ios/Runner/IncomingBackupFileBridge.swift`, `SceneDelegate.swift`, and `Info.plist`.
- Share-into-app on iOS is the `ShareExtension` target (`ios/ShareExtension/`). It runs in its own process, so the share is handed over through the app group `group.com.gilad.shadchan` — `ios/Runner/SharedProfileInbox.swift` owns that layout and is compiled into *both* targets. The extension writes image files first and the `payload.json` manifest last; the app only reads directories that have a manifest, so a half-written share is never imported. The extension then opens the app with the `shadchan-share://` scheme. Changing the app group id or the scheme means changing both entitlements files, `SharedProfileInbox.swift`, and `Runner/Info.plist` together.
- The drafts the extension produces go through the *same* `shadchan/incoming_shared_profiles` channels as the Android `SEND` intents, in the same `{id, text, filePaths}` shape, so no Dart code is platform-specific.

## Development Workflow

- Read this file before starting work and update it after meaningful project changes.
- Prefer the existing folder structure before adding new layers.
- Keep generated Hive files (`*.g.dart`) in sync when changing Hive models/enums:
  - `flutter packages pub run build_runner build --delete-conflicting-outputs`
- Run targeted checks first, then broader checks when the change touches shared behavior.
- Useful verification commands:
  - `flutter analyze`
  - `flutter test`
  - `flutter build apk --debug`
  - `flutter build apk --release` for release/platform-sensitive changes.
- iOS native compilation is not normally verifiable from this Windows environment; mention that when relevant.

## Current Capabilities To Preserve

- People management: list, search/filter/sort, add/edit/detail, favorites, gallery photos, sharing, delete warnings.
- Matches management: list, create, duplicate detection, detail, status/handler updates, notes timeline.
- Dashboard tab at `/dashboard`.
- Contacts import: explicit permission-on-entry flow, search by name/phone, multi-select import, duplicate blocking by normalized phone.
- Unified add-contacts screen at `/people/import` with toggle between swipe (default) and list views; legacy `/people/swipe` redirects to it.
- Local JSON backup/export/import from Settings.
- Open-with-app backup import for JSON backups.
- Share-to-app intake for shidduch details from WhatsApp/other apps: Android accepts text, image, or image+text shares; iOS accepts image open-with through document handling. The flow asks whether to create a new person or add to an existing person, then opens `PersonFormScreen` with the shared text in `כרטיסייה לשליחה` and shared images staged as unsaved photos.
- Birthday notifications using local timezone support.
- Hebrew privacy policy screen and Settings entry.

## Recent Notes

- 2026-07-31: The shell for AI import, built before the first source so the five sources plug into one thing rather than growing five flows. New `lib/screens/ai_import_screen.dart` at `/people/ai`, reached from `AddPeopleDialog`'s `AddPeopleMethod.ai` (which until now only showed a "בקרוב" snackbar). It lists all five sources from the brief — Excel, a WhatsApp export, pasted text, a photographed card, a gallery image — as one finished menu, with the unbuilt ones disabled and badged rather than hidden, so the page does not sprout new buttons each release. `AiImportScreen._available` is the single switch that turns a source on; it is currently empty, so **every card is disabled and the page is deliberately a dead end** until the Excel path lands. A footer states plainly what leaves the phone, because this is the one screen in a local-first app that sends contact details anywhere. New `lib/utils/parsed_person.dart` carries the multi-person result: a `ParsedCard` plus the `ParsedField`s the import had to *derive* rather than read. That distinction is the point — a gender reported because the card said בת and one reported because the name is נועה are identical JSON, and only the second is a guess. `needsReview` is true unless first name, age and gender are all present *and* all stated, which is what lets a bulk import send guesses to a human and let the rest through instead of choosing between reviewing eighty records by hand and trusting eighty blindly. Only fields a model is tempted to derive are tracked; a city is either written or it is not. Covered by `test/core/parsed_person_test.dart` (10 tests) — the rule is written down as tests rather than left to the shape of a screen, because both failure directions are bad: too strict and every record demands review, too loose and a guessed gender becomes a fact nobody checked. Verified with `dart format`, full `flutter analyze` (only the 3 pre-existing dashboard warnings + 1 pre-existing test info) and all 134 `flutter test` tests.

- 2026-07-31: Firebase + Gemini groundwork, and the first AI feature — reading a pasted shidduch card. (1) **Infrastructure.** New Firebase project `shadchan-gilad` (`shadchan` and `shadchan-app` are taken globally), billing on its own `shadchan-billing` account so AI cost is separable from pkl-guide. Added `firebase_core`, `firebase_auth`, `firebase_app_check`, `firebase_ai`; `flutterfire configure` wired the `com.google.gms.google-services` plugin and wrote `lib/firebase_options.dart` + `android/app/google-services.json`. New `lib/services/firebase_bootstrap.dart` runs first in `main._bootstrap` but is **never fatal** — it swallows its own failures into `isReady`/`failure`, because the app is local-first and a device with no network must still get everything except the AI option. It activates App Check (Play Integrity in release, `AndroidDebugProvider` with an optional `APP_CHECK_DEBUG_TOKEN` dart-define in debug) and signs in anonymously, awaiting `authStateChanges().first` with a 5s timeout rather than reading `currentUser` — on iOS the Keychain restore is async, and reading it synchronously mints a new uid on every launch (the same bug pkl-guide hit). (2) **Vertex, and why `global`.** `lib/utils/ai_config.dart` holds the model and region. The backend is Vertex AI rather than the Gemini Developer API for the Google Cloud data-processing terms — a contractual commitment not to train on or retain the content, versus a product policy. Region pinning to the EU was the other reason and had to be given up: the 3.x models are only served from the global endpoint, every EU region stops at the 2.5 family. Google's abuse-monitoring doc confirms prompts are logged only when a safety classifier flags them, for up to 90 days, opt-out-able by form. (3) **`lib/services/ai_card_parser.dart`.** Gemini is a *fallback behind* `CardParser`, never a replacement: the local regex parser still runs on every keystroke, so labelled cards stay instant, free and entirely on-device, and the model is only reached on an explicit tap (`_handleCardTextChanged` is an `onChanged` handler — a call from there would have fired per character). Only the pasted text is sent; nothing is read out of Hive. Output goes through the new `CardParser.sanitize`, so the model faces the same plausibility gate as the local pass (age 15–99, height 120–220cm, phone normalization) — it reported `age: 300` faithfully for a card that said so, and the client dropped it. (4) **Two findings worth keeping.** Marking every schema property `optionalProperties` let the model silently stop writing: on `gemini-3.5-flash-lite` the same card returned nine fields once and three the next time at temperature 0, with `finishReason: STOP` — not truncation. Every property is now **required and nullable**, forcing an explicit per-field decision; keep it even on a model that looks stable without it, because the failure was silent and under-filled the form. Separately, an instruction of only "never infer" made the model drop ages and cities the card stated plainly while still inventing a marital status, so the prompt is now two-sided: take everything stated including bare comma-list values, and withhold only what it is tempted to invent. (5) **Gender is inferred from the first name** (product decision): unambiguous Hebrew names resolve, genuinely unisex ones like שי stay null. When batch import lands, an inferred gender is what should route a record to review. (6) **Wiring.** `PersonFormScreen` gained `_applyParsedCard`, extracted from `_handleCardTextChanged` so both parsers reach the fields by one route; a `TextButton` appears under the card field when `AiCardParser.isAvailable` and text is present. Visibility is driven by a listener on `_descriptionController`, not the `onChanged` callback — that path returns early when the local parse is empty, which is exactly when the AI button is needed. (7) **iOS** is registered (`GoogleService-Info.plist` written by hand, since flutterfire's iOS steps need macOS); the file still has to be added to the Runner target in Xcode, though `DefaultFirebaseOptions.currentPlatform` means Firebase initializes without it. App Check enforcement is deliberately still off, with one debug token registered for the Pixel_3a emulator. Model is `gemini-3.6-flash`: 30/30 on the gender rule across five cards, and one identical result over three runs on every extraction case. (8) **Coverage.** New `test/core/services/ai_card_parser_test.dart` (15 tests) exercises the gates between the model's answer and the database, entirely offline against JSON strings — the model's quality is checked against the real API, but what happens to a bad answer is what nobody would notice breaking. It covers a well-formed answer, an all-null answer, unrecognised enum values, numbers sent as text, non-JSON and non-object replies, and the range gates in `CardParser.sanitize`. Writing it surfaced a real bug: `_readInt` rounded a height of `1.78` to `2` before `sanitize` saw it, and 2 then read as two metres — a 178cm person became 200cm. Heights now go through `_readHeightCm`, which applies the metres/centimetres rule to the raw JSON value. Verified end-to-end on the emulator (App Check, anonymous auth and a live Gemini round trip), plus `dart format`, full `flutter analyze` (only the 3 pre-existing dashboard warnings + 1 pre-existing test info) and all 124 `flutter test` tests.

- 2026-07-31: A wide UI pass plus the app's gender voice. (1) **Gendered text.** New `lib/utils/gender_text.dart`: sentences are written once as `'{זכר|נקבה}'` templates and resolved with `'…'.forGender(gender)`; `UserGenderContext` (an extension on `BuildContext` in `user_profile_provider.dart`) supplies the matchmaker's own gender and returns null rather than throwing when the provider is absent, so shared widgets still draw in isolation. `EmptyState` resolves its `title`/`subtitle`/`buttonText` itself, so callers just pass templates. All 43 `MatchmakerTips` were rewritten from plural imperative to gendered singular templates, and the onboarding welcome, the tip heading (שדכן/שדכנית), the empty-reminders line, `AddPeopleDialog`, `ReligiousLevelsSettingsScreen`, `ReminderPickerSheet` and the profile's notes hint follow. Everywhere the wording was a bare masculine *button* label it became the app's existing genderless verbal-noun style instead (`הוסף חברים`→`הוספת חברים`, `הצג הכל`→`הצגת הכל`, `שמור`→`שמירה`, `בחר בחורה`→`בחירת בחורה`, …). Note for future work: `context.userGender` uses `watch`, so it is only legal inside `build` — `ReminderPickerSheet` keeps its error string as an unresolved template and resolves it where it is drawn. Covered by `test/core/gender_text_test.dart`, which also asserts every tip resolves cleanly for both genders. (2) **Home.** `HomeActivityCard`'s action line wraps to two lines like its name already did; `HomeConfig.activityCardHeight` grew 74→88 and the card lost its full pill radius (26) so the wrapped text has room, with every card in the strip still one fixed box. The couples banner dropped "מאחלים לכם המשך דרך יפה!" (height 108→96). `HomeTipStrip` is now centred, larger (15.5) and written in the copper ink, headed `טיפ ל{שדכן|שדכנית}`, with the refresh icon replaced by a "next tip" arrow. **RTL arrows:** Material's chevrons carry `matchTextDirection: true`, so `Icons.chevron_left` renders pointing *right* in this app — the stats button and the dating banner now pass `Icons.chevron_right` to draw a left-pointing arrow. (3) **The wave** under "חברים ששווה לחשוב עליהם" (`HomeWaveBackground`) became stateful: the crest is sampled from two sine components whose phase is driven by the enclosing `Scrollable`'s position (published through a `ValueNotifier` so the circles above never rebuild) over a slow idle bob, plus three timed droplet splashes per cycle. (4) **Profile screen.** New `lib/screens/profile_screen.dart` at `/profile`; `SettingsScreen` was deleted and every one of its sections moved here, so the home app bar has no gear — it carries `UserProfileAvatar` (the user's photo, tapped to open the profile; add/replace/remove from the header) in place of the old time-of-day icon. `UserProfileProvider` gained `setPhotoPath`, `isSingle`/`setIsSingle` and `personalCard`/`setPersonalCard`; when the matchmaker marks themselves single the screen reveals "רווק/ה? שמור כאן את הכרטיס האישי שלך ושתף אותו בקלות בעת הצורך." with an editor and a share action (`ShareUtils.shareText`, `PhotoPickerService.pickSinglePhoto`). Child routes moved to `/profile/religious-levels` and `/profile/whatsapp-message`. (5) **Stats.** `MonthlyStatMetric` (in `monthly_stats.dart`) now owns each number's title/icon/colour/explanation plus `matchesFor`/`peopleFor`; every card on `MonthlyStatsScreen` is tappable and opens the new `StatDetailScreen` at `/stats/month/:metric` with the actual records and a per-metric trend. "הידעת?" moved above the months tracker, and the grid's aspect ratio dropped to 0.86 with a 3-line label so "זוגות שהתחילו לצאת" is not cut. (6) **Matches.** The heart in המאגר שלי opens `openSuggestionsSheet` — the same `_SuggestionsPage`, raised as a 90%-height modal sheet (`asSheet`: transparent scaffold, close button instead of back) so closing it returns to the list in place; the profile's own button still pushes a page. Suggestions are now ordered by `updatedAt` inside each availability group, so a candidate just updated in the app leads. On the profile, "ערוך נוסח בקשת פרטים" became a quiet pencil `IconButton` on the same row as the WhatsApp request. The board menu item carries "יופיע בלוח המעקב האישי בעמוד הבית." via `HomeBoardActions.menuItemChild` (only while not yet pinned; `menuItemHeight` gives the two lines room). On the רעיונות screen the add button leads and search moved to the actions. The waiting→open auto-return was already wired through `onPersonStatusChanged`/`syncMatchesForPerson`; `match_status_sync_test.dart` gained a case covering *all* of one candidate's ideas. Verified with `dart format`, `flutter analyze` (only the 3 pre-existing dashboard warnings + 1 pre-existing test info), all 109 `flutter test` tests, and `flutter build apk --debug`.

- 2026-07-31: Profile card viewing, proposal history, and a pass over the confirmation bars. (1) Tapping the profile photo now opens the new `lib/dialogs/person_card_viewer.dart` full screen — every photo swipeable with pinch-zoom, dots and a counter, and the card text over the bottom, collapsed to four lines with a "הכרטיס המלא" toggle that opens it into its own scroll area. It watches `PersonRepository`, so a photo deleted or text edited elsewhere is reflected without reopening. The old avatar tap (expand the inline card and scroll to it) and its `_showCardInline`/`_cardSectionKey` plumbing are gone; the inline "הצג כרטיס מלא" expander on the profile stays as it was. (2) The card share button moved out of the expanded inline card into the profile header beside the photo — the header row now reserves an equal-width slot on the other side so the photo stays centred. It still opens the same WhatsApp share sheet. (3) Closing a proposal now writes **two** history entries per candidate instead of one packed sentence: "נסגרה הצעה עם שושנה" and, separately, "שושנה דחתה כי הוא תורני מדי עבורה". `_historyLine` became `_outcomeReasonLine`, which returns null when there is nothing to add beyond the closing (unknown ender, no note); with a reason it drops the object ("דחתה כי…"), without one it keeps it ("דחתה את ההצעה"). The two entries are stamped a millisecond apart because `getEventsForPerson` sorts on `createdAt` and both were landing in the same millisecond, which let the reason drift away from its closing — `PersonRepository.logEvent` and the `logPersonEvent` callback took an optional `createdAt` for this. Also fixed a real gap: the status sheet's "נדחה" option only called `updateStatus` plus a free-text note, so closing a proposal that way recorded **nothing** on either person's history. It now goes through `recordOutcome` like the proposal screen does, via the extracted `lib/dialogs/match_outcome_dialog.dart` (was `_OutcomeDialog`, private to `match_detail_screen.dart`). Covered by `test/core/services/proposal_history_test.dart`. (4) Removed the "action completed" snackbars across the app — board pin/unpin, primary-photo change, suggestion dismissal, person-card save, WhatsApp message save/reset, backup and Excel "מוכן לשיתוף" (the share sheet is already open at that point), and contact removal from the import list. What deliberately stays: every error and blocked-action message, the three bars carrying a "ביטול" undo (deleted note, removed idea, handled reminder), the backup-import result with its counts, and the "בקרוב" notice which is the only feedback that entry point has. Verified with `dart format`, `flutter analyze`, and `flutter test` (101 passing).

- 2026-07-31: iOS release-readiness pass, bringing the iOS side up to the Android side. (1) Added `ios/Podfile`, which never existed because the project was created on Windows — `platform :ios, '15.5'`, the standard Flutter podhelper wiring, and a `post_install` block that pins every pod to 15.5 and sets the permission_handler compile flags (`PERMISSION_PHOTOS=1`, all others `0`) so the binary carries no permission API without a matching usage description. (2) Bundle identifier changed from the `com.example.shadchan` placeholder to `com.gilad.shadchan` in all three build configurations (and `com.gilad.shadchan.RunnerTests` for the test target), matching the Android `applicationId`. (3) `IPHONEOS_DEPLOYMENT_TARGET` raised `14.0` → `15.5`, forced by the Firebase pods (15.0) and `google_mlkit_face_detection` (15.5); iOS 14 cannot resolve the current pod set at all. (4) `Info.plist`: `CFBundleDisplayName` is now `שדכן` (was `Shadchan`) to match the Android launcher label, `CFBundleLocalizations` declares `he` so system-supplied UI comes up in Hebrew/RTL like the app itself, and `ITSAppUsesNonExemptEncryption=false` stops the export-compliance prompt on every upload. (5) Added `ios/Runner/PrivacyInfo.xcprivacy` (no collected data, no tracking, no required-reason APIs in the Runner target's own code) and wired it into the Xcode project's file references, Runner group and resources build phase. Still outstanding and only doable from the Firebase console: there is no iOS app registered in project `shadchan-gilad`, so `DefaultFirebaseOptions.currentPlatform` throws on iOS and the AI import flows stay hidden there (`FirebaseBootstrap` swallows it, so nothing else breaks) — fix with `flutterfire configure --platforms=ios`. Verified with `flutter analyze` and `flutter test`; iOS native compilation is not verifiable from this Windows environment.

- 2026-07-31: Added the iOS `ShareExtension` target, closing the last platform gap — Android's `SEND` / `SEND_MULTIPLE` intake now has an iOS counterpart instead of open-with only. New `ios/ShareExtension/` (`ShareViewController.swift`, `Info.plist`, `ShareExtension.entitlements`) and `ios/Runner/SharedProfileInbox.swift`, the shared app-group layout compiled into both targets. The extension has no compose UI: it collects text, links and up to 20 images (handling the file-URL, `Data` and `UIImage` shapes senders use), writes them into `group.com.gilad.shadchan` with the manifest last, and opens the app via the new `shadchan-share://` scheme — falling back to the responder chain because `extensionContext.open` is unsupported for share extensions, and losing nothing if both fail since the app also drains on launch and on `UIScene.willEnterForegroundNotification`. `IncomingSharedProfileBridge.drainSharedInbox()` copies the files into the app's own temp directory, deletes the group copy, and emits the same `{id, text, filePaths}` drafts the Android side does, so no Dart change was needed. `SceneDelegate` was reworked to route the new scheme before the image/backup checks. Xcode project changes: the new app-extension target with Debug/Release/Profile configurations based on `Generated.xcconfig` (so the extension's version stays pinned to the app's, which upload validation requires), an "Embed App Extensions" copy phase and target dependency on Runner, and `CODE_SIGN_ENTITLEMENTS` on both targets. The extension is deliberately absent from the `Podfile` — it links only UIKit/Foundation. Needs one console step before it works: the `group.com.gilad.shadchan` App Group must be created in the Apple Developer portal and enabled on both app ids. Verified with `flutter analyze`, `flutter test`, and a structural check of `project.pbxproj` (no dangling or orphaned object references, balanced sections); iOS native compilation is not verifiable from this Windows environment.

- 2026-07-31: Reminder-handling pass. (1) `MatchDetailScreen`'s pair card no longer prints a candidate's city — age and outlook only. (2) The `עדכון הצעה` tiles lost their hint line; the label is the whole tile and wraps onto a second line (`maxLines: 2`, no ellipsis) instead of being cut, and `_ProposalAction.hint` is gone. (3) `RemindersList` (the bell panel and the reminders screen) now lists only reminders that have come due (`ReminderAlerts.isDue`), and the single `טופל` button became two: `הזכר להמשך`, which opens the new `ReminderSnoozeDialog` (מחר / שבוע / שבועיים / חודש / 3 חודשים / בחירת תאריך) and re-dates the reminder, and `מחק`, which clears it with the usual undo snackbar. A person's reminder keeps its WhatsApp shortcut as a third action. Empty state now reads "אין תזכורות להיום". (4) On the proposals screen, cards under `ביקשת שנזכיר לך` no longer show the status tag or the "handled" tick — `MatchIdeaCard` gained `highlighted`, which gives the card the secondary accent (tinted fill, 1.6px border, soft shadow) so the ones needing attention today stand out. `onReminder`, `onUpdateStatus`, `onMarkReminderHandled` and the card's `_Actions` row were removed as unused.

- 2026-07-31: "רעיונות פתוחים" on the home screen is now only what can be moved forward today, with a due-reminder alert. New `lib/utils/home_open_ideas.dart` (`HomeOpenIdeas.build`) reuses `matchProposalTabFor` and keeps only `MatchProposalTab.open` proposals — so `בהמתנה`, a proposal whose side is busy/on a break, a dating couple and anything closed all drop out — and sorts a due reminder to the head of the row (earliest first), everything else newest-first. New `lib/utils/reminder_alerts.dart` (`ReminderAlerts`) stores, per card id in the Hive `settings` box, the reminder date that was already looked at: `isAlerting` is true only while a due reminder has not been opened since, `markSeen` is called from `MatchDetailScreen.initState`'s post-frame callback, and `MatchRepository.deleteMatch` calls `forget`. A new reminder date arms the badge again. The badge itself is `HomeAlertBadge` in the card's top-start (top-right in RTL) corner. Inside the proposal, `_ReminderCard`'s subtitle now reads `AppDateUtils.remindedAgoLabel` — "עברו 3 ימים מאז שביקשת שנזכיר לך" — in the alert colour, with the reminder note appended after it. Note for future tests: a Hive write from inside a `testWidgets` fake-async zone never completes and hangs the whole suite, which is why the wording is covered by a unit test on `remindedAgoLabel` (`test/core/reminder_wording_test.dart`) rather than by opening a due proposal in a widget test; ordering and badge rules are covered by `test/core/home_open_ideas_test.dart` and `test/core/reminder_alerts_test.dart`.

- 2026-07-30: Fixed cream-on-cream app bar titles. The app-wide `AppBarTheme` bakes `appBarForegroundColor` into `titleTextStyle`, and an explicit `titleTextStyle` colour wins over `AppBar.foregroundColor` — so every bar that overrides `backgroundColor` to the profile's cream canvas rendered its title invisible. Added `_profileAppBarTitleStyle(theme)` in `person_detail_screen.dart` and passed it as `titleTextStyle` on the four cream bars (profile with its pinned name, `התאמות`, `עריכת כרטיס`, `היסטוריה`). Any future bar on a light background must do the same. Covered by a widget test that opens התאמות from a profile and asserts the title's resolved colour is dark (it reads 0.98 luminance without the fix).

- 2026-07-30: Three follow-ups. (1) Home order is now הלוח שלי → רעיונות פתוחים → חברים ששווה לחשוב עליהם → הפעולות האחרונות שלך (then the couples banner, the month button and the tip). (2) The heart on a row in `PeopleScreen` opens the profile's own התאמות screen instead of a picker sheet: `person_detail_screen.dart` exposes `openSuggestionsFor(context, personId)` which pushes the same `_SuggestionsPage`, and the people screen keeps only the "יש לבחור מגדר" guard. The two now-dead entry points, `lib/dialogs/match_suggestion_flow.dart` (`MatchSuggestionFlow`) and `lib/widgets/match_suggestions_view.dart` (`MatchSuggestionsSheet`/`MatchSuggestionsView`, already unreferenced), were deleted so there is one matches experience. (3) On a suggestion row the card expander moved out of the round-button row onto the name line as `_CardExpanderButton` — the actions stay `לא מתאים` + `פתיחת הצעה`, with no crowded third icon.

- 2026-07-30: Home screen tweaks. (1) `HomeActionCards` swapped the pair: `הוסף חברים` is now the first child (right in RTL, flex 11) and `הוסף רעיון` second (left, flex 9); both round arrows moved to `AlignmentDirectional.centerStart` and point the other way — `HomeArrowButton` gained an `icon` parameter (default `chevron_left`, the action cards pass `chevron_right`). (2) `HomeActivityCard` in `הפעולות האחרונות שלך` lets a long name wrap to a second line (`maxLines: 2`, line height 1.15) instead of ellipsizing on one; the card box stays fixed at `HomeConfig.activityCardWidth/Height`, so a wrapped card is exactly the size of its neighbours. Added a widget test covering the wrap plus identical card sizes.

- 2026-07-30: Redrew `הלוח שלי` on the home screen as a pinned note board. `HomeMiniCard` was replaced by `HomeBoardNote` in `lib/widgets/home_section.dart`: paper-tinted tile (a stable pastel per item from `AppColors.softYellow/softRose/softGreen/softBlue/softSand/softPurple`, picked by a `tintSeed` hash so an item keeps its colour), barely-rounded corners, a small tape strip at the top, the avatars centred under it, the name below them, then the pinned note and reminder — and along the bottom edge `HomeNoteActionsButton`, a full-width `פעולות` pill that is the `PopupMenuButton` child, replacing the old `more_vert` corner icon. The whole row now runs inside `HomeNoteBoard`, a framed strip (warm `secondaryLight` wash + `secondary` hairline border, radius 22) so the notes read as pinned to one board. `HomeConfig.cardWidth/cardHeight` grew to 152×186 for the extra button, and `boardFramePadding` was added. Rewrote `test/widgets/home_section_test.dart` around the note (fits its box, grows with the system font, the frame runs wider than the notes).

- 2026-07-30: Removed the "waiting for details" limbo across the app — a contact is now either completed into the database or discarded, never parked. `ContactsImportService.stageSingleCandidate` returns a `StagedContact` (`person` + `isNewRecord`) and the new `discardStagedCandidate` hands it to `PersonRepository.discardContactDraft`, which deletes a draft written for this run and, for a reused soft-deleted contact, only clears `needsReview` and leaves the record hidden with all of its data. Every staging path now calls it when `QuickUpdateDialog` is cancelled: `_addSingle` and `_addSelected` in `ImportContactsScreen`, and `_importAcceptedCandidate` in `SwipeImportScreen` (which also discards when the swipe was undone or the screen went away mid-flow; the candidate still returns to the review-later pile). `PeopleScreen._completePendingContactDrafts` — the legacy `מחכה למילוי פרטים שלך` banner, which now only ever shows drafts from older versions or a restored backup — discards the cancelled draft too, so the banner can empty and never refills. Dead paths that created pending people were deleted: `ContactsImportService.importSingleCandidate`, `importSelections`, `ContactImportSelection`, `ContactImportResult` and `PersonRepository.restoreHidden` (none had callers). `PersonPickerSheet`'s "הוספה למאגר" now creates the person with `needsReview: false` — a visible person is a regular contact from that moment, even with only a name; the "להשאיר ככה" option stays hidden and keeps the flag (it drives the WhatsApp details-request text). Verified with `dart format`, full `flutter analyze` (only the 3 pre-existing dashboard warnings + 1 pre-existing test info), all 76 `flutter test` tests (rewritten `contacts_import_service_test.dart` staging/discard coverage), and `flutter build apk --debug`.

- 2026-07-30: Replaced the selectable no-photo avatar galleries with exactly one fixed default per gender: `assets/male_pic/default_male_avatar.png` and `assets/female_pic/default_female_avatar.png`. Both are the user-approved flat, faceless silhouettes with cream ovals, stone blue for men and muted rose-mauve for women. `PersonAvatarAssets` now contains one path per gender, so every legacy `avatarIndex` also normalizes to the same fixed image; the persisted Hive/backup field remains only for backwards compatibility. Removed all nine old avatar PNGs, deleted `PersonAvatarPicker`, removed its state and controls from both person editors, and changed the empty photo hint to state that the default illustration will be used. Updated `person_avatar_assets_test.dart` to assert the exact one-per-gender invariant and legacy-index normalization. Verified with targeted test/analyze, full `flutter analyze` (only the 3 pre-existing dashboard warnings + 1 pre-existing test info), all 76 `flutter test` tests, and `flutter build apk --debug`.

- 2026-07-30: List-mode contact import (`ImportContactsScreen`) — the selection bar's `לא רלוונטי` is now `הסר`, and its confirmation no longer blocks the screen. `_markSelectedIrrelevant` grabs the `ScaffoldMessengerState` before the Hive write and routes through the new `_showBriefSnackBar`, which `clearSnackBars()` first (nothing can queue behind it), shows a floating one-second bar with no action button, and arms a guard `Timer` that closes the controller by hand if the messenger's own timer never fires. The old 3-second bar with the `ביטול` action (and `_restoreMany`) is gone: it sat over the selection row and read as a freeze. Removal stays reversible — a removed contact is still findable by search, and adding them clears the skip.

- 2026-07-30: Rebuilt `MatchDetailScreen` in the exact visual language of the `רעיונות` screen — cream canvas, white cards, hairline borders, very soft shadows, and the same thin rose/blue `_EdgeStripe` on the two outer edges instead of a separate frame or background per gender. Top is one shared `_PairCard`: photo in a gender ring, name, `גיל · השקפה · עיר`, a small WhatsApp button and the person's global `ProfileStatusTag` under each side, the heart between them, and a single derived line at the bottom (`פתוחה · יאללה לקדם`, `בהמתנה · כרמל בהפסקה · לבדוק שוב ב־27.8`, `יוצאים`, `תזכורת · …`) built by `_deriveSituation` — no second explanatory sentence. Tapping a photo or the details opens that profile; tapping WhatsApp opens a two-option sheet naming both people (`פתיחת שיחה עם X` / `שליחת הכרטיס של Y אל X`, the second backed by the new `WhatsAppUtils.sendCardTo`, disabled when the other side has no saved card). Below it `_ReminderCard` is its own clear area (empty state `הוספת תזכורת`; with a reminder the date, relative label and note; when due a quick row of `עדיין בהמתנה` / `חזר-ה לפנוי` / `טופל`). Then `_UpdateProposalCard`: one area, three pastel `_ActionTile`s (`_ActionTone` go/wait/stop) with `יוצאים` in the middle, larger and the only one with a frame. The related-contact row is now a quiet `+ איש קשר שקשור להצעה` text button until a contact exists, then the existing card. Person reminders gained an optional note (`PersonReminders.noteFor` / `set(..., note:)`, `PersonRepository.setPersonReminder(..., note:)`), asked for right after the date through the new `ReminderNoteDialog` (with `דילוג`) on every path — status change, reminder edit, `העברה להמתנה` and rescheduling from `RemindersList` — and shown on the person reminder card. `MatchRepository.setWaiting` now takes an optional reason (sheet leads with `בלי סיבה מיוחדת`) plus an explicit `reminderNote`. Global availability behaviour (proposals moving to/from `בהמתנה`) was already wired through `onPersonStatusChanged` and is unchanged. Verified with `dart format`, full `flutter analyze` (only the 3 pre-existing dashboard warnings + 1 pre-existing test info), all 75 `flutter test` tests (updated match-detail expectations, new WhatsApp-menu test), and `flutter build apk --debug`.

- 2026-07-30: Rebuilt `HomeScreen` as a hierarchy of differently shaped blocks instead of one repeated white card, keeping the existing palette (stone blue / copper / cream) and every existing action. New `lib/widgets/home_panels.dart` holds the wide blocks: `HomeHeroBand` (soft gradient opening band, painted heart line, bundled illustration couple, `הצג רעיונות חדשים` CTA), `HomeActionCards` (filled blue `הוסף חברים` at flex 11 next to a frameless warm `הוסף רעיון` at flex 9; stacked while the database is under `HomeConfig.compactActionsFromPeopleCount`), `HomeDatingBanner` (one wide festive banner with a `PageView` between couples, dots, and the fixed line `שומרים על קשר :) כל זוג צריך חבר אחד שיאמין בו`), `HomeStatsButton` (opens `/stats/month`; the numbers no longer appear on home) and `HomeTipStrip` (larger tip text). `lib/widgets/home_section.dart` gained `homeScaled`, a `height` on `HomeCarousel`, `HomeActivityCard` (low pill strip), `HomeIdeaCard` (short centred idea card), `HomeSuggestionBubble` + `HomeWaveBackground` (free circles on a soft wave) and `HomeArrowButton`; `HomeMiniCard` is now only the board's card. Sections renamed: `חזרה מהירה` → `הפעולות האחרונות שלך`, `אולי שווה לחשוב עליהם` → `חברים ששווה לחשוב עליהם`. `HomeSuggestions` was rewritten around `HomeSuggestionReason`, whose declaration order is the priority order (returned-to-available → candidates found → new → details added → card updated → no idea → idea closed → card ready → long silence → stale card → open ideas → neutral); every sentence is gender-aware and backed by a record (status changes come from `PersonEvent`s via the new `PersonRepository.getAllEvents()`, hand edits from `RecentActivityStore`), with a neutral fallback instead of an invented fact. New `/ideas/new` route → `NewIdeasScreen` + `lib/utils/new_idea_suggestions.dart`: pairs the database against itself with `MatchSuggestionUtils`, drops pairs that already have any proposal or were dismissed, scores them, caps `maxPairsPerPerson`, and opens a real proposal through `MatchRepository.create`. Added `AppDateUtils.elapsedLabel`. New tuning constants in `HomeConfig`. Coverage: rewritten `test/core/home_suggestions_test.dart`, new `test/core/new_idea_suggestions_test.dart` and `test/widgets/home_panels_test.dart`. Verified with `dart format`, full `flutter analyze` (only the 3 pre-existing dashboard warnings + 1 pre-existing test info), all 74 `flutter test` tests, and `flutter build apk --debug`.

- 2026-07-27: Journal edit mode + status filter fit. (1) `_FilterPill` in `PeopleFiltersSheet` gained a `dense` flag (horizontal padding 6, `labelPadding: EdgeInsets.zero`) used by the `סטטוס` row, and every pill label is now a `FittedBox(scaleDown)` instead of an ellipsized `Text` - so `בהפסקה` shows in full next to `פנוי`/`תפוס` on one row even at 320dp (it only shrinks a hair below ~340dp). (2) The `יומן ההצעה` header has a pencil that turns the timeline into a multi-select list (checkboxes, selected rows tinted, `_JournalEditBar` at the bottom with a red `מחיקת N הערות` plus `ביטול`); a long press on any entry enters the same mode with that entry ticked. While editing, the pencil is replaced by an `X` (tooltip `סיום עריכה`) that leaves edit mode, `PopScope` makes system Back leave edit mode before it leaves the screen, and the note composer is hidden. Bulk delete removes the checked notes with one snackbar `ביטול` that restores the whole batch through `MatchRepository.restoreNote`. Added widget coverage for the pill row and for the pencil/long-press/X cycle. Verified with `dart format`, targeted `dart analyze`, all 48 `flutter test` tests, full `flutter analyze` (only the 3 pre-existing dashboard warnings), and `flutter build apk --debug`.

- 2026-07-27: Follow-up interaction fixes on `MatchDetailScreen`. (1) A journal note now opens a single `_NoteEditorDialog` on a short tap (the old flow was long-press → bottom sheet → separate edit dialog / confirm dialog): the dialog holds the editable text plus `מחיקה` / `ביטול` / `שמירה`. Deleting no longer asks to confirm first; it deletes and offers `ביטול` in a snackbar, backed by the new `MatchRepository.restoreNote`, which re-writes the note with its original id and `createdAt`. (2) The availability control on the pair card is a `PopupMenuButton<ProfileStatus>` anchored under the status pill (`selectableProfileStatuses` = available/busy/onBreak, current value check-marked) instead of a small bottom sheet. (3) `ReminderPickerSheet.show` is now `isScrollControlled` with a 90%-height cap so every option is reachable, and `דלג` moved to the top of the list, above the intervals, with a `בלי תזכורת` subtitle. Note for future tests: a Hive write triggered from inside a `testWidgets` fake-async zone never completes, which makes `Hive.close()` in `tearDownAll` hang for the whole suite - assert on the dialog/UI and close without writing, or the write needs `tester.runAsync` (which then surfaces the app's `MissingPluginException` event channels). Verified with `dart format`, targeted `dart analyze`, all 46 `flutter test` tests, full `flutter analyze` (only the 3 pre-existing dashboard warnings), and `flutter build apk --debug`.

- 2026-07-27: Tidied the `MatchDetailScreen` layout per the user's mockup. (1) The pair card shows each candidate's full name (up to 3 lines instead of a single ellipsized line) and the per-side controls were reordered to status pill → circular soft-green WhatsApp button, matching the reference. (2) `_SituationCard` was restructured so its buttons have fixed roles: header (icon/title/detail), a tappable `_ReminderTile` row showing the full date plus a relative Hebrew label (`היום`/`מחר`/`בעוד N ימים`/`עברו N ימים`), a `_DueChoiceBlock` (`מה המצב עכשיו?` + `עדיין בהמתנה`/`חזר/ה לפנוי`) shown only when a reminder is due, then a divider with the emphasized action as one full-width `FilledButton` and the remaining actions as an evenly split `TextButton` row (no more mixed `Wrap`). (3) The buried `+ איש קשר שקשור להצעה` text link became `_RelatedContactsCard`, a card that is always visible: the first contact row (WhatsApp + `+N` badge, tap opens the full list) above an explicit add row. (4) Removed the `הוספת הערה` journal button that only focused the note field; the section is now just the `יומן ההצעה` title above the timeline and composer. Added a phone-size (360×780) widget regression test covering un-truncated names, the reminder and add-contact rows, and the removed journal button. Verified with `dart format`, targeted `dart analyze`, all 45 `flutter test` tests, full `flutter analyze` (only the 3 pre-existing dashboard warnings), and `flutter build apk --debug`.

- 2026-07-27: Restyled the `PeopleFiltersSheet` to match the supplied compact mobile reference. The taller rounded cream bottom sheet now has a centered title, separate white cards for gender/age/religious style/status, explicit `הכל` gender selection, consistent outlined/selected pill controls, a labeled range slider with compact endpoint boxes, a card-like collapsible `סינון מורחב` area for height and marital status, and a fixed bottom action row with copper `הצג תוצאות` plus outlined `נקה`. Existing multi-select behavior, enabled/custom religious styles, dynamic age bounds, fixed 120–200 cm height range, filter normalization, and active advanced filters were preserved. Added widget coverage for initial selection, advanced expansion, and clearing. Verified with targeted analysis/test, all 44 `flutter test` tests, full `flutter analyze` (only the 3 pre-existing dashboard warnings), `flutter build apk --debug`, and `git diff --check`.

- 2026-07-27: Redesigned `MatchDetailScreen` as a compact proposal workspace in the same cream/stone-blue/pastel visual language as `רעיונות`. The top pair card now shows both candidates, photos, compact details, per-person WhatsApp, profile navigation, and the shared `ProfileStatusTag` control. Person availability changes are global: busy/on-break statuses pause relevant open proposals, returning both sides to available reopens automatically paused proposals, dating marks both people busy, and marriage marks both `מזל טוב`. Busy/on-break changes from proposal detail, profile detail, or the full person form immediately offer a reminder sheet with tomorrow/week/two-weeks/month/three-month presets, `עוד חודש` marked as the unselected recommendation, free-text Hebrew offsets, and a direct calendar picker. Proposal detail now has one derived situation card (open/waiting/needs-attention/dating/closed), due-reminder quick actions, no duplicate status/progress controls, compact multi-contact support with optional relationship descriptions and WhatsApp, and a journal filtered to real proposal events/notes rather than technical field/reminder changes. `MatchContact.description`, waiting/progress state, and related contacts are preserved in JSON backup/restore; regenerated the Hive adapter. Added parser, repository-sync, backup, and widget regression coverage. Verified with targeted analysis, all 43 `flutter test` tests, full `flutter analyze` (only the 3 pre-existing dashboard warnings), `flutter build apk --debug`, and `git diff --check`.

- 2026-07-27: Refined `PersonDetailScreen` into a compact in-page profile flow. The availability switcher now reuses the exact `ProfileStatusTag` visual language from `המאגר שלי` (including the expanded choices). Removed the profile-only bottom action bar; person profile routes keep the app's regular `בית` / `המאגר שלי` / `רעיונות` bottom navigation, while WhatsApp (labelled with the person's first name), emphasized `התאמות`, and subtle `לפתיחת הצעה` actions now sit in the page before the card. `הצג כרטיס מלא` and avatar taps expand/scroll to the full saved card inside the profile instead of pushing the old full-screen card page; the expanded card has a share icon that opens a compact WhatsApp share sheet and sends the full saved card text through WhatsApp's recipient picker. Removed the old `הכרטיס שלו`/WhatsApp heading and the notes-title icon. Open proposals are now one neutral compact list with only the other person's name, proposal status, and a small WhatsApp shortcut for that other person. Added router and widget regression coverage. Verified with targeted `dart analyze`, the profile widget test, full `flutter analyze` (only the 3 pre-existing dashboard warnings), all 38 `flutter test` tests, and `flutter build apk --debug`.

- 2026-07-26: Contact imports now require complete quick details before entering the database. `QuickUpdateDialog` edits locally, has explicit `ביטול`/`אישור` actions, and requires name, gender, an enabled religious style, and age; cancelling does not partially save. List and swipe imports first persist a hidden `needsReview` contact draft, activate it only after confirmation, and a cancelled swipe is returned to the deferred review pile. `PeopleScreen` uses `databaseCount` and shows a top `מחכה למילוי פרטים שלך` banner for hidden contact-import drafts; opening it walks through the pending drafts and only activates each one after valid confirmation. Draft filtering is limited to import sources so hidden temporary proposal people are not included. Added service and widget coverage. Verified with targeted analysis/tests, `flutter analyze` (only the 3 pre-existing dashboard warnings), full `flutter test` (37 pass), and `flutter build apk --debug`.

- 2026-07-26: Added hidden-contact review to the list-mode add-contacts filter sheet. Next to `מוסתרים כרגע: N`, the compact `צפייה במוסתרים` action opens `HiddenContactsDialog`, listing each automatically filtered contact with name/phone and an individual `הצגה` action. Restored contacts are not imported immediately; their normalized phones are persisted under `revealed_filtered_phones` in the shared `swipe_skipped_phones` box, so they return to import results and remain visible on future loads. Explicit `לא רלוונטי` contacts stay out of this dialog, and the swipe deck also honors restored auto-filter contacts on its next load. Added a widget regression test for individual restoration. Verified with targeted `dart analyze`, full `flutter analyze` (only the 3 pre-existing dashboard warnings), all 35 `flutter test` tests, and `flutter build apk --debug`.

- 2026-07-26: Navigation visibility + WhatsApp details-request crash fix. `_AppShell` now shows the app-level `BottomNavigationBar` only for the exact primary paths `/home`, `/people`, and `/matches`; nested people/match pages and the dashboard render without it (profile-specific action bars remain). Fixed the profile missing-card flow's Navigator/Overlay assertion in two places: `_requestDetails` validates and touches the local person record before launching the external WhatsApp app, avoiding a provider-driven root-Navigator rebuild during app-resume/overlay teardown; and the reusable `DetailsMessageDialog` now owns/disposes its `TextEditingController` with the dialog widget lifecycle instead of disposing it immediately when `showDialog` completes. Added regression coverage for the exact primary-path rule and safe dialog closure. Verified with targeted `dart analyze`, full `flutter analyze` (only the 3 pre-existing dashboard warnings), all 34 `flutter test` tests, and `flutter build apk --debug`.

- 2026-07-26: General settings/filter/avatar refinements. (1) Religious-style choices now consistently come from `ReligiousLevelsProvider` in the full person form, compact contact update dialog, person-card editor, people filters, and match-proposal filters. The clean-install defaults are exactly חרדי / דתי לאומי תורני / דתי לאומי / דתי פתוח / חילוני; styles disabled in Settings are no longer kept as hidden active filter choices, and custom labels configured under "אחר" are supported by both people and match filters. (2) The people height filter now always uses the fixed 120–200 cm product range instead of deriving its bounds from existing records. (3) The accept/open-proposal action in both match-suggestion views uses a heart outline instead of a plus. (4) No-photo people now use bundled gender-specific illustrated avatars from `assets/male_pic/` and `assets/female_pic/`. `Person.avatarIndex` (HiveField 28) stores the user's choice; old cards receive a stable pseudo-random choice from person id + gender, photos always take precedence, and the add/edit photo sections expose the avatar chooser only while no photo is present. The selection is included in JSON backup/restore. Regenerated `person.g.dart`. Verified with targeted `dart analyze`, full `flutter analyze` (only the 3 pre-existing dashboard warnings), all 32 `flutter test` tests, and `flutter build apk --debug`.

- 2026-07-23: In `MatchIdeaCard`, moved each side's WhatsApp shortcut from beside the name/age text to a centered position directly beneath it, and enlarged the icon to 22px. Verified with `dart format` and targeted `dart analyze`.

- 2026-07-22: Fixed a startup crash on upgrade + hardened startup/backup. (1) Root cause: a tester's data written by an older app version was missing the non-nullable `Person.needsReview` (HiveField 20) field, so the generated adapter's `fields[20] as bool` cast `null` and threw `type 'Null' is not a subtype of type 'bool'` during `Hive.openBox<Person>` — before any migration ran, showing a black screen. Fix: added `defaultValue:` to every non-nullable Hive field that legacy records can lack, so the generated read falls back instead of crashing — `Person` fields 11 (`photosPaths` → `[]`), 12 (`isFavorite`), 16 (`profileStatus` → `ProfileStatus.available`), 20 (`needsReview`), 23 (`hidden`); `MatchIdea` 3 (`status` → idea) and 4 (`currentHandler` → me); `PersonNote`/`MatchNote` field 4 (`isAutomatic`). Regenerated the `*.g.dart` adapters. When adding future non-nullable Hive fields, always give them a `defaultValue` so older data still opens. (2) `main.dart` was restructured with `runZonedGuarded` + a try/catch around `_bootstrap()`; on any startup failure it now shows `_StartupErrorApp` (RTL screen with the error text + stack trace) instead of a black screen, and notification/migration work can't block launch. `PersonMigrations.convertBirthDatesToAges` iterates a `.toList()` copy with per-record try/catch. (3) Rewrote `lib/services/backup_service.dart`: the old format silently dropped `Person.hidden`/`needsReview` (hidden people reappeared on restore) and `MatchIdea.reminderDate`/`reminderNote` (all reminders lost) — now every field is exported; import is crash-safe (per-record try/catch, lenient enum/date parsing via `_enumByName`/`_date`, records with no id skipped and counted, unknown backup `version` no longer hard-fails), and old backups with a `birthDate` still convert to an age. Export `version` bumped to 2. (4) Fixed a latent crash I'd introduced: `RemindersScreen` used `DateFormat(..., 'he')` while `intl` locale data is never initialized — replaced with a manual Hebrew month formatter. Verified with `dart format`, `flutter analyze` (only the 3 pre-existing dashboard warnings), and `flutter test` (30 pass, incl. new backup round-trip + malformed-record tests).

- 2026-07-22: Home/מאגר/matches/swipe batch. (1) `HomeScreen` was rewritten to a compact screen: greeting moved into the AppBar title (time-of-day + name), which also gained תזכורות (`/reminders`) and search actions; search opens an in-AppBar field (hint `חיפוש במאגר שלי`) that filters the whole database live. The body is only the `בוא/י נחשוב` subtitle, the dashboard stats, `הוספת חברים`, and a new full-width `הוספת רעיון` card (→ `/matches/add`). All the old featured-card / pending-card / friends-list / home sort+filter machinery was removed, so `HomeScreen` now takes just `initialSearch` and the `/home` route no longer parses page/seed/sort. (2) New `RemindersScreen` at `/reminders` (`lib/screens/reminders_screen.dart`) centralizes every `MatchIdea` with a `reminderDate`, sorted by date with overdue/today/soon labels; tapping opens the proposal. (3) `PeopleScreen` is now `המאגר שלי`: AppBar `+` opens `AddPeopleDialog` (`lib/dialogs/add_people_dialog.dart`, the same three add methods as home); the search row has `סנן`/`מיין` text buttons plus a view-toggle; the FAB was removed. (4) People filters gained height (range slider, bounds from existing `heightCm`) and marital-status (multi-select, gender-neutral `filterLabel`) filters in `PeopleFiltersSheet`, wired through `PeopleFilterState` into `PeopleScreen`'s filtering, chips, and reset. (5) Gender colors: `AppColors.genderAccent`/`genderSurface` (men = stone blue, women = muted rose `#A9748A`/`#EFDDE4`). Applied to `PersonListCard`, the `MatchesScreen` idea squares, and `MatchDetailScreen` side cards. In `MatchDetailScreen` the sides are now laid out by gender (woman right, man left in RTL) and each side card has a WhatsApp button beneath it (`_SideWhatsAppButton`). (6) `CreateMatchScreen` passes `allowCreateOutsideDatabase: true` to `PersonPickerSheet`, which now shows `הוספת מישהו שלא במאגר`: a name dialog with `הוספה למאגר` (visible, `needsReview`) or `להשאיר ככה` (hidden) — both create a `Person` and return it for the match. (7) Swipe import (`SwipeImportScreen`): up-swipe / a new `דלג` button defers a contact to a review-later pile offered after the deck ends (`_deferredCandidates`, deck rebuilt via `_deckGeneration` key); left-swipe is now `לא רלוונטי` (the permanent skip); and each accepted swipe opens `QuickUpdateDialog` like the list flow. Verified with `dart format`, `flutter analyze` (only the 3 pre-existing dashboard warnings remain), `flutter test` (28 pass), and `flutter build apk --debug`.

- 2026-07-21: Navigation/home/manual-add batch. (1) Bottom nav is now `בית` / `המאגר שלי` (`Icons.group`, goes to the `/people` branch) / `רעיונות`; the middle tab no longer jumps to `/people/import`. (2) `HomeScreen` greets by time of day (`בוקר טוב`/`צהריים טובים`/`ערב טוב`/`לילה טוב` + user name) and gained an `הוספת חברים` section under the stats with three tiles: `הוספה מאנשי הקשר` (`/people/import`), `הוספה ידנית` (`/people/add`), and an inert `היעזר ב-AI להוסיף מאגר אישי קיים` placeholder. (3) `AddContactsScreen` has two segments (`החלקה`, `רשימה`); the `ידני` segment was removed and leaving pops when possible. (4) `PersonFormScreen` was reordered to photos → `כרטיסייה לשליחה` (hint `הדבק כרטיסייה כאן`) → name → religious style → status → gender → age → height → marital status → location → inquiry contact → personal notes. Pasting a card auto-fills the fields below through the new `lib/utils/card_parser.dart`; it only writes fields the user has not typed into (`_autoFilledFields`). The inquiry contact has a `בחירה מאנשי הקשר` button backed by the new `lib/widgets/device_contact_picker_sheet.dart`. (5) New `Person` fields `heightCm` (HiveField 25) and `maritalStatus` (HiveField 26, new `MaritalStatus` enum, typeId 9, registered in `main.dart`), wired into backup JSON and the Excel `אנשים` sheet (which replaced its two birth-date columns with גובה/מצב משפחתי). (6) Birth dates were removed from the whole app per the user: no date UI in the form or the profile edit page, the dashboard `ימי הולדת` section and `_MonthlyBirthdaysSection` are gone, and `NotificationService.scheduleBirthdayNotifications` was replaced by `cancelBirthdayNotifications()`, which clears previously scheduled ids 10000-19999. A person's age is now only `manualAge` + `manualAgeUpdatedAt` (which advances it once a year); `Person.age` no longer falls back to a birth date. The four birth-date Hive slots were renamed to `legacyBirthDate` / `legacyHebrewBirth*` and exist solely so `PersonMigrations.convertBirthDatesToAges` (`lib/services/person_migrations.dart`, run from `main.dart` before `runApp`) can convert them to an age once and null them out; a `migratedBirthDatesToAges` flag in the settings box keeps it to a single pass, an age already entered by hand wins over a computed one, and the Hive indices 4/17/18/19 must not be reused. Backup export no longer writes birth-date keys, but import still converts a legacy `birthDate` into an age so older backup files keep their ages. The unused `PersonRepository.getBirthdaysToday`/`getUpcomingBirthdays` helpers were removed. Verified with `dart format`, `flutter analyze` (only the 3 pre-existing dashboard warnings remain), `flutter test` (28 pass, including new `test/core/services/card_parser_test.dart` and `test/core/services/person_migrations_test.dart`), and `flutter build apk --debug` (run before the birth-date removal; not re-run after).

- 2026-07-06: Replaced the app palette again with the user's preferred blue-stone/copper palette. `AppColors.primary` is now smoky stone blue `#7F9EAA`, `AppColors.primaryDark` is `#5F7F8C`, action/highlight `AppColors.secondary` is pastel copper `#C1845B`, light background is oat cream `#F7F0E4`, section rows are `#EFE4D4`, warm cards/search fields stay `#FFFDF8`, primary text is coffee black `#211D17`, secondary text is `#7C7468`, dividers are `#E2D7C8`, and status/tag helper colors were updated to the matching powder-blue/sage/yellow/lilac/coffee-milk/olive/mustard/taupe palette. `AppTheme` now uses secondary/copper for filled/elevated action buttons while primary blue remains the base navigation/icon color. Splash colors were updated in `pubspec.yaml` and Android 12 styles. Verified with `dart format`, targeted `dart analyze`, `flutter test test/widget_test.dart`, stale-color `rg`, and `git diff --check`.
- 2026-07-06: Removed the `כרטיס` tab from `PersonDetailScreen` per user request. The person detail tabs are now only `התאמות` and `הצעות`; card viewing remains available by tapping the profile avatar, and card editing remains available from the AppBar edit action / legacy edit route. Removed the now-unused tab preview/photo-management widgets and related permission/image-picker helpers from `person_detail_screen.dart`. Verified with `dart format lib/screens/person_detail_screen.dart`, `dart analyze lib/screens/person_detail_screen.dart`, `flutter test test/widget_test.dart`, and `git diff --check`.
- 2026-07-06: Fixed a `MatchesScreen` regression where the open ideas tab could show one stretched proposal instead of a compact list with all names. The proposal row widget is now `MatchIdeaListCard` with a fixed 88px height before its stretched inner row, so `ListView` measures every idea as a normal compact row. Added a widget test that renders two match idea cards and asserts all four Hebrew names/ages are visible. Verified with `dart format`, targeted `dart analyze`, `flutter test test/widget_test.dart`, and `git diff --check`.
- 2026-07-06: Replaced the app palette with warm chamra colors requested by the user; those color values were later superseded by the blue-stone/copper palette note above. `AppTheme` sets matching primary/secondary container colors so Flutter defaults do not leak purple tones, and profile-page local constants plus matches gender tiles use the shared palette/auxiliary colors.
- 2026-07-06: Large UX/design batch. (1) Theme/font: the app font is `Google Sans` (OFL, Hebrew-supporting, bundled in `assets/fonts/`, registered in `pubspec.yaml` and applied via `AppTheme`); color values were later superseded by the warm palette note above. (2) `PersonDetailScreen` was restructured: three tabs `התאמות` / `הצעות` / `כרטיס`; the הצעות tab holds the open/rejected proposal groups that used to sit under the card tab; the inline-edit machinery was removed in favor of a dedicated `_PersonCardEditPage` (pushed from the AppBar edit icon, the card's `עריכה` button, and the legacy `/people/:id/edit` route via `initiallyEditing`); the card-edit form no longer shows birth-date fields (data preserved); tapping the profile avatar opens `_PersonCardViewPage` (full-screen photo pager with arrows + send-card below + share/WhatsApp/edit actions); the AppBar shows the person's name once the header scrolls away (compact pinned bar); deleting a person pops back instead of `go('/people')`. (3) Suggestion rejection is now a soft dismissal persisted per source person in the Hive `settings` box under `suggestionDismissals.<personId>` (`lib/utils/suggestion_dismissals.dart`) — no rejected `MatchIdea` is created and dismissed candidates just sink to the end of the suggestions list (both in the profile tab and `MatchSuggestionsSheet`). Suggestion lists order paused (תפוס/בהפסקה) candidates after available ones within each tier; suggestion rows have a quick-view expander (photo + card text, only when the candidate has a card) and tapping a candidate avatar opens their profile. (4) Home: friends list groups by availability (פנוי → הפסקה → תפוס) before the chosen sort; the featured random contact ignores call-log recency and prefers a random person with both a photo and a card each launch. (5) `MatchesScreen` idea rows are compact two-square cards (male left/pastel blue, female right/pastel pink, heart between, name+age above a small photo). (6) `MatchDetailScreen` was minimized: tapping a side card opens the profile (no `צפה בכרטיס` buttons), the status area is just the chip wrap without a title, the reminder moved to an AppBar bell-with-plus icon, the `אחראי נוכחי` section was removed entirely (handler data/model kept), the notes journal is chat-bubble styled, and `MatchRepository.create` no longer writes the automatic `הצעה נפתחה` note. Person notes (`יומן הערות`) now support edit/delete per note (`PersonRepository.updateNote`/`deleteNote`). Stale test expectations were fixed (`widget_test.dart` bottom-nav labels, chardal in `match_suggestion_utils_test.dart`). Verified with `flutter analyze` (3 pre-existing dashboard warnings remain), `flutter test` (all pass), and `flutter build apk --debug`.

- 2026-06-05: Redesigned the `הצעות` screen (`MatchesScreen`) into tabs instead of the active/archive toggle + status-filter sheet. Tabs are `הצעות פתוחות`, `בהמתנה`, `יוצאים`, `יצאו`, `נדחו`, and `חתונות` (the weddings tab only appears when there are married matches). Tab placement is derived per match by `matchProposalTabFor()` in `lib/utils/enums.dart`: terminal/progress statuses (married/dated/rejected/dating) map to their tabs; an `idea`/`checking`/`unavailable` proposal goes to `בהמתנה` when its status is `unavailable` or when one side's personal-card status pauses matches (`ProfileStatus.pausesMatches` = `busy`/`onBreak`), otherwise `הצעות פתוחות`. Non-terminal proposals involving an archived-profile (`mazelTov`) person are hidden, preserving prior active-view behavior. Renamed `MatchStatus.unavailable.displayName` from `צד לא פנוי` to `בהמתנה`. In `MatchDetailScreen` the status chips now use `_selectableStatuses` (ordered to the tabs, `בבדיקה` omitted so an open proposal is just `רעיון`) and the obsolete "הועברה לארכיון" snackbar was removed. The `/matches` route still accepts `statuses`/`archived` query params and maps them to the matching initial tab. Verified with `flutter analyze` on touched files and `dart format`. NOTE: the working-tree copy of `person_detail_screen.dart` is mid-edit and currently does not compile (missing `flutter_card_swiper` import and `_ProfileCardTab`/`_SuggestedMatchesTab` called with undeclared params), so `flutter test` could not be run; left untouched.
- 2026-06-05: Restyled `PersonDetailScreen` to match the provided Hebrew profile HTML reference. The profile page now uses a warm cream canvas, a centered rounded profile header with large avatar, compact personal summary, status pill, primary WhatsApp/matches actions, secondary share/edit actions, warmer notes CTA, redesigned `כרטיס לשליחה` card, and matching list/suggestion card styling. Verified with `dart format lib/screens/person_detail_screen.dart`, `dart analyze lib/screens/person_detail_screen.dart`, `flutter test test/widget_test.dart`, and `git diff --check`.
- 2026-06-05: Changed `HomeScreen` friends pagination from local "load more" behavior to real route-based pages. The home route now accepts `page`, `seed`, `sort`, and `q` query parameters; pressing `עמוד הבא` pushes `/home?page=N...` and shows the next 20 people while preserving the per-launch random seed so contacts do not repeat between pages. Verified with `dart format lib/screens/home_screen.dart lib/utils/app_router.dart`, `dart analyze lib/screens/home_screen.dart lib/utils/app_router.dart`, `flutter test test/widget_test.dart`, and `git diff --check`.
- 2026-06-05: Implemented the HomeScreen friends-list sort and search actions. The sort button now opens a Hebrew bottom sheet matching the PeopleScreen sorting options while preserving the home screen's default per-launch random order, and the search button toggles an inline Hebrew name search with an empty-results state. Verified with `dart format lib/screens/home_screen.dart`, `dart analyze lib/screens/home_screen.dart`, `flutter test test/widget_test.dart`, and `git diff --check`.
- 2026-06-05: Updated the dashboard `רעיונות` stat in `lib/widgets/dashboard_summary.dart` to count everything visible in the active `הצעות` view, including `יוצאים`, while excluding archived matches and matches whose people are archived. The stat now links to `/matches` instead of a narrower status-filtered route. Verified with `dart format lib/widgets/dashboard_summary.dart`, `dart analyze lib/widgets/dashboard_summary.dart`, `flutter test test/widget_test.dart`, and `git diff --check`.
- 2026-06-05: Made the dashboard data area on `HomeScreen` compact to match the provided reference: the home stats now show only the number and Hebrew title in small flat tiles, without icons or subtitle text, and omit the birthdays section to take less vertical space. `DashboardScreen` still uses the full shared dashboard summary. Verified with `dart format`, targeted `dart analyze`, `flutter test test/widget_test.dart`, and `git diff --check`.
- 2026-06-05: Added the dashboard data view to `HomeScreen` by extracting the existing dashboard content into shared `lib/widgets/dashboard_summary.dart`. `DashboardScreen` now uses the same shared slivers, and Home shows the same Hebrew stat labels, counts, routes, wedding banner, and Hebrew-month birthday section with a refreshed rounded-card design. The home view keeps showing the empty-state CTA below the stats when no people exist. Verified with `dart format` on touched Dart files, targeted `dart analyze`, `flutter test test/widget_test.dart`, and `git diff --check`.
- 2026-06-05: Extracted the shared people row card into `lib/widgets/person_list_card.dart` and updated both `HomeScreen` and `PeopleScreen` to use it, so the home people list matches the card/list-tile styling from the people screen: directional padding, larger avatar, profile-status emoji in the title, subtitle/missing-info tags, favorite star, match action, and WhatsApp action with the same enabled/disabled styling. The home list disables the row Hero because the featured card can show the same person on the same route. Verified with `dart format` on touched Dart files, targeted `dart analyze`, `flutter test test/widget_test.dart`, and `git diff --check`.
- 2026-05-10: Added dual display modes to the `התאמות` tab in `PersonDetailScreen`. Suggested matches can now be viewed either as the existing Hebrew list or as a swipe deck backed by `flutter_card_swiper`; swiping right/pressing `כן` counts a positive review and opens the existing candidate action sheet, while swiping left/pressing `לא` skips the candidate for the current pass. The swipe view includes yes/no counters and a restart option after the deck ends. Verified with `dart analyze lib/screens/person_detail_screen.dart`, `flutter analyze`, `flutter test test/widget_test.dart`, `flutter test`, and `git diff --check`.
- 2026-05-10: Added an automatic WhatsApp prompt to `MatchDetailScreen` when a proposal is opened. The dialog asks `תרצה לשלוח להם ווטסאפ?` and offers separate Hebrew actions for opening WhatsApp with the male side and female side, plus `בהמשך`; invalid/missing phone numbers are shown as disabled actions. The prompt appears once per opened match-detail screen instance and uses the existing `WhatsAppUtils.openChat()` flow. Verified with `dart analyze lib/screens/match_detail_screen.dart`, `flutter analyze`, `flutter test test/widget_test.dart`, `flutter test`, and `git diff --check`.
- 2026-05-10: Updated the `התאמות` tab interaction in `PersonDetailScreen`. Tapping a suggested match now opens a bottom sheet with the candidate avatar/name and two Hebrew choices: `צפייה בכרטיס`, which navigates to that person's profile, and `פתיחת הצעה`, which reuses the existing create/open match flow. Verified with `dart analyze lib/screens/person_detail_screen.dart`, `flutter test test/widget_test.dart`, and `git diff --check`.
- 2026-05-10: Moved the person-level notes timeline out of the main profile card into a dedicated `יומן הערות` page opened from a full-width button directly under the `PersonDetailScreen` profile header. The notes page shows the requested privacy notice, `ההערות כאן הן לצפייה אישית שלך בלבד.`, above the existing personal notes timeline/add-note controls and keeps reading/writing through `PersonRepository` so updates remain local and reactive. Verified with `dart analyze lib/screens/person_detail_screen.dart`, `flutter analyze`, `flutter test test/widget_test.dart`, `flutter test`, and `git diff --check`.
- 2026-05-10: Moved the person-card share action into the top profile header on `PersonDetailScreen` and made both `שיתוף` and `וואטסאפ` visible labeled buttons there. The share action still uses the existing `ShareUtils.sharePerson()` flow for sharing the card/photos, while WhatsApp still opens a direct chat through `WhatsAppUtils`. The duplicate AppBar share icon was removed. Verified with `dart analyze lib/screens/person_detail_screen.dart`.
- 2026-05-10: Moved profile status updates back to the main profile header. `PersonDetailScreen` now shows the current `ProfileStatus` as a tappable Hebrew status chip under the profile details; tapping it expands the other statuses directly below, and selecting one calls `PersonRepository.updateProfileStatus()`. The old duplicate status section was removed from the inline edit area. Verified with `dart analyze lib/screens/person_detail_screen.dart`.
- 2026-05-10: Added a visible Hebrew `עריכה` button to the top profile header in `PersonDetailScreen`. The button switches to the display-card tab and opens the existing inline personal-details edit form, so editing is available from the profile itself without relying only on the AppBar icon or the card-specific edit action. Verified with `dart analyze lib/screens/person_detail_screen.dart`.
- 2026-05-10: Added persistent per-person manual filtering to the `התאמות` tab on `PersonDetailScreen`. The tab now has an accessible Hebrew `סינון` button, shows whether automatic or personal filtering is active, and saves changed filters in the local Hive `settings` box under the existing `matchProposalFilters.<personId>` key. If no personal filter exists, the tab keeps using the automatic religious/age compatibility rules; once a filter is saved, that person's suggestions and direct `התאמות` picker use the saved age/religious/status filters. Verified with targeted `dart analyze`, `flutter test test/core/services/match_suggestion_utils_test.dart`, and `flutter test test/widget_test.dart`.
- 2026-05-10: Redesigned `PersonDetailScreen` into a profile-first view instead of opening directly into inline editing. The top area now shows the person photo/avatar, name, age, religious style and status, followed by swipeable tabs for `כרטיס לתצוגה`, automatic `התאמות`, `רעיונות פתוחים`, and `רעיונות שנשללו`. The share-card tab presents photos and `כרטיסייה לשליחה` as read-only display by default, while the existing inline edit form remains available from the Hebrew edit action. Verified with targeted `dart analyze` and `flutter test test/widget_test.dart`.
- 2026-05-10: Added automatic match suggestions from each person. People list cards, people table rows, and person detail now expose a Hebrew `התאמות` action that opens a filtered candidate picker and creates or opens the matching proposal. The fixed filter logic lives in `lib/utils/match_suggestion_utils.dart`: religious style follows the requested compatibility groups, and age is evaluated from the male-side rules for both directions so female-source suggestions are reversed correctly. `PersonPickerSheet` now accepts a custom candidate predicate for complex filters. Verified with targeted `dart analyze` on touched Dart files and `flutter test test/core/services/match_suggestion_utils_test.dart`.
- 2026-05-04: Added share-to-app intake for WhatsApp-style profile details. Android `MainActivity` now separates backup JSON intents from shared profile text/images via a new incoming shared profiles Method/EventChannel and manifest filters for `text/plain`, `image/*`, and multiple images. Flutter now listens globally, opens a Hebrew choice/search screen, and routes to `PersonFormScreen` for either a new person or existing person with shared text staged in `כרטיסייה לשליחה` and shared images copied into the person photo draft until save. iOS document handling now routes opened images into the same shared-profile flow, and the existing backup bridge was added to the Xcode project sources alongside the new image bridge. Privacy policy updated for shared incoming content. Verified with targeted `dart analyze`, `flutter test test/widget_test.dart`, `flutter build apk --debug`, and `git diff --check`; full `flutter analyze` still timed out in this Windows environment.
- 2026-05-04: Replaced `PendingPeopleScreen` list view with a one-at-a-time Hebrew completion workflow. The screen now opens the first pending person directly, exposes inline fields for first name, last name, profile status, gender, religious level, and estimated age, includes a WhatsApp details-request button using the existing onboarding message, provides AppBar access to the full person card, and has bottom `שמור`/`דלג` actions. Saving updates the person through `PersonRepository.update()` and clears `needsReview`; skipping only hides the person for the current screen session. Verified with targeted `dart analyze lib/screens/pending_people_screen.dart`, `flutter test test/widget_test.dart`, and `git diff --check`.
- 2026-05-04: Updated the default WhatsApp details-request message to the shorter personal wording requested by the user. Added `WhatsAppMessageSettingsScreen` at `/settings/whatsapp-message`, linked from Settings under `הודעות`, so users can edit or reset the saved WhatsApp request message. `WhatsAppUtils` now reads the custom message from the local Hive `settings` box and falls back to `defaultOnboardingMessage`. Verified with targeted `dart analyze` on WhatsApp/settings/router callers.
- 2026-05-04: Fixed a `RenderFlex overflowed` error in the people table filter headers. `_TableFilterHeader` now centers its content in a `FittedBox` with a compact filter icon so narrow columns like `סטטוס`/`גיל` can scale down instead of overflowing. Verified with `dart analyze lib/screens/people_screen.dart`.
- 2026-05-04: Match proposal age filtering now uses dynamic min/max ages from existing candidate people instead of the old hard-coded 18-50 range. `MatchProposalFilterSheet` computes bounds from non-pending, non-archived people matching the target gender and hides the age slider when no ages are available. Verified with targeted `dart analyze lib/dialogs/person_picker_sheet.dart lib/screens/person_detail_screen.dart lib/screens/create_match_screen.dart`.
- 2026-05-04: Match proposal filtering is now remembered per source person. When opening a proposal from a person card, `MatchProposalFilterSheet` loads that person's last saved age/religious/status filters from the local Hive `settings` box, shows the filter sheet for review, and only saves the new choices when the user taps `הצג תוצאות`. Verified with targeted `dart analyze lib/dialogs/person_picker_sheet.dart lib/screens/person_detail_screen.dart` and `flutter test test/widget_test.dart`.
- 2026-05-04: Moved the person detail save action from the centered FAB into the `SliverAppBar` as a Hebrew `שמירה` check icon action. The action still calls the existing `_saveAndPop()` flow and shows a compact progress indicator while saving; bottom content padding was reduced now that the FAB is gone. Verified with `dart analyze lib/screens/person_detail_screen.dart`.
- 2026-05-04: Added a global back-button guard in `lib/app.dart`. When Android/system Back would otherwise exit the app and the current route is not the root people page (`/people`), the app now navigates to `/people` first; Back from `/people` still exits normally. Normal in-app back stack pops are preserved. Verified with targeted `dart analyze lib/app.dart lib/utils/app_router.dart` and `flutter test test/widget_test.dart`.
- 2026-05-04: Person card sharing now appends inquiry contact details to the shared card text when `inquiryContactName` or `inquiryContactPhone` exists. `ShareUtils.sharePerson()` keeps sharing the same photos, but the text now adds a blank line and `לבירורים: ...` after the card description. Verified with `dart analyze lib/utils/share_utils.dart`.
- 2026-05-01: Added optional inquiry contact fields to people (`inquiryContactName` and `inquiryContactPhone`) for storing a compact בירורים contact name/phone. Updated the Hive `Person` adapter, backup JSON import/export, Excel export, privacy policy, manual person form, person detail inline edit, and people list subtitle display. Verified with targeted `dart analyze` on touched files and `flutter test test/core/services/backup_service_test.dart test/core/services/excel_export_service_test.dart`.
- 2026-05-01: Fixed the manual person form save FAB layout. The `FloatingActionButton.extended` on `PersonFormScreen` now uses a stadium shape and `centerFloat` location so the Hebrew `שמור` label sits correctly in the button instead of being squeezed by the global circular FAB theme.
- 2026-05-01: Manual person creation now supports selecting gallery photos in `PersonFormScreen`. The photo editor is available in both create and edit modes; new manual contacts use a stable draft person id so copied photo files are saved under the same id that will be persisted, and `photosPaths` is written when the new `Person` is created. `git diff --check` passed for the touched file; `dart format` still timed out in the current sandbox.
- 2026-05-01: Moved match reminder controls into the proposal detail screen. The matches list no longer shows the reminder bell action; `MatchDetailScreen` now has a `תזכורת להצעה` section with add/edit/delete reminder controls backed by the existing `reminderDate`/`reminderNote` fields and notification refresh flow. Attempted `dart format` on the touched files, but it timed out in the current sandbox.
- 2026-05-01: Person detail inline edits now autosave more aggressively. Text fields schedule a quiet debounce save, focused fields save when focus leaves, gender/religious-level/birthday changes save immediately, lifecycle pause/inactive states trigger a quiet save, and opening a match proposal first saves pending edits before checking gender. Back navigation now autosaves instead of asking whether to discard changes. Added a centered save FAB on person detail that saves silently and pops back without a Snackbar. Attempted `dart format` and `flutter analyze`, but both timed out in the current sandbox after the permission/context switch.
- 2026-05-01: Added a global keyboard-dismiss wrapper from `lib/main.dart`. The app now unfocuses the active input when the user taps non-interactive space anywhere in the app, making it easier to hide the keyboard on iOS while preserving normal button/field interactions. Verified with `flutter analyze`.
- 2026-05-01: Changed the unified add-contacts screen toggle to include three clear options: `החלקה`, `רשימה`, and `ידני`. Removed the separate manual-add AppBar icon; selecting `ידני` opens `/people/add`. The segmented control now uses explicit selected/unselected colors so selected labels and icons are readable in light mode. Verified with `flutter analyze`.
- 2026-05-01: Updated the primary add FABs on people and matches screens to use centered extended FABs with the plus icon and Hebrew label `הוסף`. Verified with `flutter analyze`.
- 2026-04-27: Added multi-photo gallery selection for person photos in both person detail and the legacy person form edit flow using `ImagePicker.pickMultiImage()`. Person photos now support choosing a primary photo by moving that path to the front of `Person.photosPaths`; existing preview/avatar/header behavior continues to read the first photo as primary. The detail photo strip, edit photo strip, and full-screen `PhotoViewer` expose Hebrew primary-photo controls, and the privacy policy now mentions selecting one or more photos. Verified with `flutter analyze` and `flutter test`.
- 2026-04-27: Improved people table view. `_PeopleTable` is now stateful, shows a short Hebrew loading spinner before rendering, and supports header-based table filters for gender, age range, religious level, and profile status. Gender/religious/status filters allow multi-select values; age uses a range selector. Removed the city column from the table and kept name cells as plain tappable text without edit clutter. Verified with `flutter analyze` and `flutter test`.
- 2026-04-27: Updated the matches list search row so the status filter button uses the same `Icons.tune` visual as the people screen while keeping the active-filter dot indicator on the left side of the RTL search row. Verified with `flutter analyze`.
- 2026-04-27: Reviewed `SwipeImportScreen` and fixed two edge cases. Undoing an accepted swipe before the async import finishes now marks the history entry as undone and deletes the imported person once the import completes, preventing stray pending people. Returning from device settings after granting contacts permission now reloads contacts instead of showing an empty state. Verified with `flutter analyze` and `flutter test`.
- 2026-04-24: Added Settings export to Excel. `ExcelExportService` uses the `excel` package to generate an `.xlsx`, then rewrites workbook XML through `archive` so Hebrew sheet names and `rightToLeft="1"` are applied, following the pattern from `pkl_guide`. The export includes sheets for `סיכום`, `אנשים`, `יומן אנשי קשר`, `הצעות`, and `יומן הצעות`, and is shared from Settings via `ייצוא לאקסל`. Added a service test that inspects the generated workbook XML. Verified with `flutter analyze` and `flutter test`.
- 2026-04-24: Contacts list import now returns directly to `/people` after selected contacts finish importing, without showing a completion dialog. The people list refreshes through the existing repository state after import. Verified with `flutter analyze` and `flutter test`.
- 2026-04-24: Person detail and edit now share a single screen. `PersonDetailScreen` can enter inline edit mode from the existing card or from `/people/:id/edit`; the old edit route now opens the detail screen with editing enabled instead of `PersonFormScreen`. Exiting inline edit with `צא` or the device back action saves changes automatically before returning to the read-only card. Verified with `flutter analyze` and `flutter test`.
- 2026-04-24: People list cards now show compact missing-info tags for important incomplete fields. `PeopleScreen` displays `חסר מגדר` when `Gender.unknown` is stored and `חסר גיל` when neither birth date nor manual age is available, while preserving the existing age/religious-level subtitle. Verified with `flutter analyze`.
- 2026-04-24: Added a WhatsApp contact button to person detail using `font_awesome_flutter` for the official WhatsApp icon. The button converts saved phone numbers to WhatsApp international format, opens `wa.me` externally with a prefilled Hebrew onboarding message, and shows a Hebrew error when no valid phone is stored. Added `PhoneUtils.toWhatsAppNumber` coverage. Verified with `flutter analyze` and `flutter test`.
- 2026-04-24: Dashboard proposal stats now split the old ideas count into `רעיונות פתוחים` and `רעיונות שנפסלו`. Open ideas count statuses `idea`, `checking`, and `unavailable`; rejected ideas count only `rejected`. Verified with `flutter analyze`.
- 2026-04-24: Match status `צד לא פנוי` (`MatchStatus.unavailable`) now remains in the active proposals list instead of moving to the archive. Only rejected, dated, and married proposal statuses are archived by `MatchStatus.isArchived`. Verified with `flutter analyze`.
- 2026-04-24: Improved the people search/filter button contrast. The filter button in `PeopleScreen` now uses the primary color with `onPrimary` icon color instead of the pale primary container, making the tune icon readable in light and dark themes. Verified with `flutter analyze`.
- 2026-04-24: Added a user-selectable app theme mode. `ThemeModeProvider` stores `system`/`light`/`dark` in the local Hive `settings` box, `App` applies the selected `ThemeMode`, and Settings now includes a Hebrew segmented control for `מערכת`/`בהיר`/`כהה`. Verified with `flutter analyze` and `flutter test`.
- 2026-04-24: Added a person-level notes timeline and quick profile status controls. `PersonNote` now stores contact-card notes in a dedicated Hive box (`person_notes`) with backup/export/import support, while legacy `Person.notes` remains readable in the timeline for older data. Person detail now exposes profile status chips near the top and records status changes as automatic person notes. The old private-notes row was removed from person detail and the notes text field was removed from person edit. Generated `person_note.g.dart` with build_runner. Verified with `flutter analyze` and `flutter test`.
- 2026-04-24: Simplified birthday display and expanded Hebrew birthday support. Person detail now shows one birth-date row with automatic Hebrew/Gregorian conversion instead of separate duplicate rows, and the top quick-info chips were removed from person detail. Dashboard now includes a `ימי הולדת החודש העברי` section with tappable people for the current Hebrew month. Hebrew birthday notifications now fall back to converted Gregorian birth dates when stored Hebrew fields are missing and schedule upcoming Hebrew birthday reminders for multiple years. Verified with `flutter analyze` and `flutter test`.
- 2026-04-24: Polished person-card wording and edit flow. Gender display labels now use `זכר`/`נקבה`, the share action sends only the saved send-card description text without prepending the person's name, and the description UI is labeled `כרטיסייה לשליחה`. Person edit now includes a photo picker/thumbnail strip; newly picked photos are kept as unsaved form changes until the user saves, with abandoned copied photos cleaned up on discard. Verified with `flutter analyze` and `flutter test`.
- 2026-04-24: Improved large contact-book import performance. `ContactsImportService` now precomputes normalized blocked-name keywords, processes contacts in small async batches with progress callbacks, and stores filtered import candidates in a local Hive cache (`contact_import_cache`) so repeat entries can render quickly while a refresh runs. List and swipe import screens now show Hebrew progress text/linear loading instead of appearing frozen. Privacy policy text was updated to mention the local import-candidate cache.
- 2026-04-24: Added stronger contact import narrowing for large address books. Candidate contacts now require Israeli mobile prefixes (`05` or `+9725`), existing people remain filtered out before display, names matching `lib/utils/names.dart` keywords are hidden by default in the list import screen but still searchable and can be shown with a Hebrew toggle, and the swipe import screen excludes those name-filtered candidates. Android swipe import now sorts candidates by recent call log order through a native `READ_CALL_LOG` method channel with graceful fallback. Privacy policy text was updated for call log usage. Verified with `flutter analyze`, `flutter test`, and `flutter build apk --debug`.
- 2026-04-24: Updated the contacts list import flow so existing contacts are filtered out before display, selected contacts import with `Gender.unknown` by default instead of asking for gender, and import candidate cards stay compact without expanding to a gender selector. Also removed two stale analyzer warnings in dashboard/person detail. Verified with `flutter analyze` and `flutter test`.
- 2026-04-24: Rewrote `AGENTS.md` to match the current flat Flutter structure (`models`, `providers`, `screens`, `services`, `utils`, `widgets`, `dialogs`) and removed the long outdated implementation history. Verified the current file layout, `pubspec.yaml`, router, app entry points, and iOS deployment target while updating these instructions.
- 2026-04-26: Fixed three swipe import screen issues. (1) Corrected RTL icon positions so the heart (add) appears on the right and X (skip) on the left, matching swipe directions. (2) Skipped cards are now persisted in a Hive box (`swipe_skipped_phones`) so they do not reappear after app restart. (3) Added an undo button (center, between heart and X) using `CardSwiperController.undo()` with `onUndo` callback; undo reverts the counter state and, for accepted cards, deletes the imported person. The undo button is visually disabled when no history is available. Verified with `flutter analyze`.
- 2026-04-26: Added multi-select status filter to matches screen. A filter icon button in the AppBar opens a `showModalBottomSheet` with `FilterChip`s for each status relevant to the current view (active statuses: רעיון, בבדיקה, צד לא פנוי, יוצאים; archive statuses: נדחה, יצאו, חתונה). The filter dot badge on the icon shows when a filter is active. The filter resets when toggling between active/archive views. An empty filter set shows all matches. When filtered results are empty a dedicated Hebrew empty state with a clear-filter button is shown. Verified with `flutter analyze`.
- 2026-04-26: Redesigned dashboard stats layout. Changed "זוגות שיצאו" icon from `Icons.history` to `Icons.heart_broken`. Moved "חתונות" out of the grid into a separate full-width `_WideStatCard` banner below the 6-item 2×3 grid. Verified with `flutter analyze`.
- 2026-04-26: Reordered person detail screen sections: profile status → photos → כרטיסייה לשליחה (standalone) → action buttons → inline edit form → notes → related matches → details. Removed city (עיר) and source (מקור היכרות) fields from the UI only; Hive data is still loaded, saved, and exported. Verified with `flutter analyze`.
- 2026-04-26: WhatsApp button now conditionally sends the onboarding message. People with a filled description (כרטיסייה לשליחה) open a plain chat; people without a description get the prefilled onboarding message asking for details and photos. Verified with `flutter analyze`.
